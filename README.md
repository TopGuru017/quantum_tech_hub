
  # Clone website with React

  This is a code bundle for Clone website with React. The original project is available at https://www.figma.com/design/PimrFezkmwDLFaOz0SEEnj/Clone-website-with-React.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  